# --- dados gerados (PDFs, JSON de resultado, resumos, paginas) + PII terceiros ---
data/

# --- segredos (mantem config/.env.example e config/categoria.json rastreados) ---
config/.env
.env

# --- artefatos python / editor ---
__pycache__/
*.pyc
.venv/
venv/
.claude/*
!.claude/agents/
.DS_Store

# --- temporarios do scraping ---
cookies.txt
page.html
result*.xml
*.zip

# --- Rust / Tauri ---
src-tauri/target/
src-tauri/gen/

# --- Node / Vue (frontend em app/) ---
app/node_modules/
app/dist/

# --- Nuxt UI (modo Vue) — d.ts gerados por unplugin-auto-import/-vue-components ---
app/auto-imports.d.ts
app/components.d.ts

.vercel
