# Produção: a API é servida na MESMA origem via proxy da Vercel
# (`/api/*` → Render, ver vercel.json). VITE_API_URL vazio = chamadas
# relativas (`/api/...`), first-party → o cookie de sessão funciona em
# qualquer navegador (sem bloqueio de cookie de terceiros).
VITE_API_URL=
