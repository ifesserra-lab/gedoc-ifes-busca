# URL da API web (crate `server/`). Vazio/omisso no desktop (Tauri usa IPC).
# Dev local: aponte para o servidor axum local.
VITE_API_URL=http://localhost:8787

# Na Vercel, defina VITE_API_URL nas Environment Variables do projeto,
# apontando para a URL pública da API (container Fly/Railway).
